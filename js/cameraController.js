// cameraController.js
class CameraController {
  constructor(sceneManager) {
    this.sceneManager = sceneManager;
  }

  update() {
    // Placeholder for scroll / transition logic (future use)
  }
}

window.CameraController = CameraController;
